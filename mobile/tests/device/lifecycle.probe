test "restart app preserves state"
  @device @lifecycle
  restart the app
  wait 5 seconds
  see "FlutterProbe Test App"

test "clear app data resets everything"
  @device @lifecycle
  clear app data
  wait 5 seconds
  open the app
  wait 5 seconds
  see "FlutterProbe Test App"
