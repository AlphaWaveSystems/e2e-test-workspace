<!-- HARNESS:START
     version=0.34.0
     schema=1
     agent=e2e-test-workspace
     updated=2026-07-19T12:15:55Z
     DO NOT EDIT — regenerate with: harness-ctl update /Users/patrickbertsch/dev/e2e-test-workspace
-->

# Feature List & Verification Gate

> How this project tracks work-to-be-done and how the harness decides
> something is actually finished, for agent `e2e-test-workspace`.




This agent does not have the `verify` tool, so it cannot itself produce a
`verification_id` — features it works on can be moved to `in_progress` or
`failed`, but reaching `passed` requires another agent (or a human) to
supply verified evidence.



**Not available to this agent.** `features` is not in this agent's
`allowed_tools`. Attempting to call it will return a `forbidden` error from
the harness gateway before the tool call is even attempted.


<!-- HARNESS:END -->

---

<!-- Add project-specific feature categories, ownership conventions, or
     verification command notes below. The harness block above is managed
     automatically. -->
